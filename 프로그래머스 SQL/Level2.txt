[Level 2]
고양이와 개는 몇 마리 있을까
(정답 1)
SELECT ANIMAL_TYPE, COUNT(ANIMAL_TYPE) as count
FROM ANIMAL_INS 
GROUP BY ANIMAL_TYPE 
ORDER BY ANIMAL_TYPE

최솟값 구하기
(정답 1)
SELECT MIN(DATETIME) from ANIMAL_INS;

동명 동물 수 찾기
(정답 1)
SELECT NAME, COUNT(NAME) as COUNT
FROM ANIMAL_INS
GROUP BY NAME
ORDER BY NAME

동물 수 구하기
(정답 1)
SELECT count(ANIMAL_ID) from ANIMAL_INS;

입양 시각 구하기(1)
(정답)
SELECT HOUR(DATETIME) as HOUR, COUNT(DATETIME) as COUNTS
FROM ANIMAL_OUTS
WHERE HOUR(DATETIME) >= 9 AND HOUR(DATETIME) <= 19
GROUP BY HOUR
ORDER BY HOUR;

NULL 처리하기
(정답)
select ANIMAL_TYPE, IFNULL(NAME, 'No name') AS NAME, SEX_UPON_INTAKE
FROM ANIMAL_INS
ORDER BY ANIMAL_ID

중성화 여부 파악하기
/*
(정답 1)
SELECT ANIMAL_ID, NAME, if(SEX_UPON_INTAKE like 'Intact%', 'X', 'O') as "중성화"
from ANIMAL_INS
order by ANIMAL_ID;

(정답 2)
*/
SELECT ANIMAL_ID, NAME, IF(INSTR(SEX_UPON_INTAKE, 'Intact'), 'X', 'O') as "중성화"
FROM ANIMAL_INS
ORDER BY ANIMAL_ID ASC;

중복 제거하기
//(정답 1)
SELECT count(DISTINCT NAME)
FROM ANIMAL_INS
WHERE NAME IS NOT null;
